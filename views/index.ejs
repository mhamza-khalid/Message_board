<html>
  <head>
    <title><%= title %></title>
  </head>
  <body>
    <% messages.forEach((message, index) => {%>
        <div><%= message.text %></div>
        <div><%= message.user %></div>
        <div>
            <%= message.added %>
            <a href="/message?position=<%= index %>"><button>View message</button></a>
        </div>
        <hr></hr>
    <% }); %>
    <a href="https://messageboard-production-a94f.up.railway.app/new"> Add a new message</a>
  </body>
</html>
